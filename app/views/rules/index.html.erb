<p style="color: green"><%= notice %></p>

<h1>Rules</h1>

<div id="rules">
  <% @rules.each do |rule| %>
    <%= render rule %>
    <p>
      <%= link_to 'Show this rule', rule %>
    </p>
  <% end %>
</div>

<%= link_to 'New rule', new_rule_path %>
