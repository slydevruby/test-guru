<%= form_with(model: rule) do |form| %>
  <% if rule.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rule.errors.count, 'error') %> prohibited this rule from being saved:</h2>

      <ul>
        <% rule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: 'display: block' %>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :status, style: 'display: block' %>
    <%= form.collection_select :status, %w[category test level], :to_s, :to_s, include_blank: false %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>