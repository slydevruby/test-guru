<h2 class='mb-3'>
  <%= @test.title %>
  <%= link_to '', edit_admin_test_path(@test), class: 'bi bi-pencil' %>
  <%= link_to '', admin_test_path(@test), data: { turbo_method: :delete, turbo_confirm: t('confirm_delete') }, class: 'bi bi-trash text-danger' %>
</h2>
<h4 class="heading heading-level">Уровень:
  <%= test_level(@test.level) %>
</h4>
<ul class="question-collection">
  <%= render partial: 'admin/questions/question', collection: @test.questions %>
</ul>
<hr>
<%= link_to t('.new_question'), new_admin_test_question_path(@test), class: 'btn btn-primary' %>
<%= link_to t('.back_to_tests'), admin_tests_path, class: 'btn btn-primary' %>