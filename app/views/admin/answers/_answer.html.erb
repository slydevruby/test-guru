<li>
  <% if answer.correct %>
    <b><%= answer.body %> </b>
  <% else %>
    <%= answer.body %> 
  <% end %>
  <br>
  <%= link_to t('.edit'), edit_admin_answer_path(answer) %> |
  <%= link_to t('.delete'), admin_answer_path(answer), data: { turbo_method: :delete, turbo_confirm: t('confirm_delete') } %>
</li>
