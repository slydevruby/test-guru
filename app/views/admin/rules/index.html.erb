<%#= link_to 'New rule', new_admin_rule_path, class: 'btn btn-primary mb-3' %>
<%= link_to t('.new_rule_cat'), admin_type_rule_path(:cat), class: 'btn btn-primary mb-3' %>
<%= link_to t('.new_rule_test'), admin_type_rule_path(:test), class: 'btn btn-primary mb-3' %>
<%= link_to t('.new_rule_level'), admin_type_rule_path(:level), class: 'btn btn-primary mb-3' %>
<div id="rules" class='mt-3 d-flex gap-3 flex-wrap'>
  <% @rules.each do |rule| %>
    <%= render rule %>
  <% end %>
</div>