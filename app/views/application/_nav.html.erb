<nav class="navbar navbar-expand-lg mt-2 mb-4 navbar-dark bg-secondary rounded" data-bs-theme="light">
  <div class="container-fluid">
    <%= link_to root_path, class: 'btn btn-primary marg-right-2' do %>
      <i class="bi bi-rocket-takeoff"></i>
      Пройти тест
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_user.is_a?(Admin) %>
          <li  class="nav-item">
            <%= link_to admin_tests_path, class: 'nav-link active' do %>
              Dashboard
            <% end %>
          </li>
          <li  class="nav-item">
            <%= link_to awards_path, class: 'nav-link active' do %>
              <i class="bi bi-file-ruled"></i>
              Awards
            <% end %>
          </li>
        <% end %>
      </ul>
      <div class="d-flex" role="search">
        <% if user_signed_in? %>
          <ul class="navbar-nav align-items-center">
            <li  class="nav-item">
              <%= t('welcome', email: current_user.email) %>
            </li>
            <li class="nav-item text-muted">
              <%= link_to t('signout'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'nav-link active'  %>
            </li>
          </ul>
        <% else %>
          <ul class="navbar-nav">
            <%= link_to t('signin'), new_user_session_path, class: 'nav-link active' %>
            <%= link_to t('signup'), new_user_registration_path, class: 'nav-link active' %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>