<nav class="navbar navbar-light text-bg-primary">
  <div class="container-fluid">
    <%= link_to 'TestGuru2', root_path, class: 'navbar-brand' %>
    <span class="navbar-text">
      <% if user_signed_in? %>
        <b><%= t('welcome', email: current_user.email) %></b> |
        <%= link_to t('signout'), destroy_user_session_path, data: { turbo_method: :delete } %>
      <% else %>
        <%= link_to t('signin'), new_user_session_path %> | 
        <%= link_to t('signup'), new_user_registration_path %>
      <% end %>
    </span>
  </div>
</nav>
