<h1><%= t('.header') %></h1>
<table class="table table-hover">
  <thead>
    <tr>
      <th><%= Test.model_name.human %></th>
      <th><%= Category.model_name.human %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.total') %></th>
      <th>Прохождение</th>
    </tr>
  </thead>
  <%= render @tests %>
</table>

